import { TroubleshootingScenario } from '../types/socMasterLab';

export const TROUBLESHOOTING_PHILOSOPHY_STEPS = [
  { step: 1, title: 'Read the Exact Error', desc: 'Read the full error message in journalctl or application logs. Don\'t guess or panic.' },
  { step: 2, title: 'Check Service Status', desc: 'Run "sudo systemctl status <service>" to see if the process is active, crashed, or restarting.' },
  { step: 3, title: 'Validate Configuration Syntax', desc: 'Test configuration files before starting (e.g., suricata -T, filebeat test config, logstash --config.test_and_exit).' },
  { step: 4, title: 'Inspect Daemon Logs', desc: 'Examine logs in /var/log/ or use "sudo journalctl -u <service> -n 50 --no-pager" to locate the root cause stack trace.' },
  { step: 5, title: 'Verify Listening Sockets', desc: 'Use "sudo ss -tulpn" to confirm the daemon bound to the expected port and IP address (0.0.0.0 vs 127.0.0.1).' },
  { step: 6, title: 'Test Network Connectivity', desc: 'Verify local and remote connectivity using "ping", "nc -zv", or "curl -I". Ensure host firewalls (UFW) permit traffic.' },
  { step: 7, title: 'Verify Authentication & TLS', desc: 'Check usernames, passwords, service account tokens, and SSL certificate verification flags.' },
  { step: 8, title: 'Fix Exactly ONE Thing at a Time', desc: 'Change a single configuration value, save, and retest. Changing multiple variables creates new unidentifiable errors.' },
  { step: 9, title: 'Retest and Verify the Fix', desc: 'Restart the service and verify with the exact verification command. Ensure health is restored.' },
  { step: 10, title: 'Document the Solution', desc: 'Record the symptom, root cause, and command in your engineering runbook or SOC portfolio.' },
];

export const DIAGNOSTIC_WORKFLOW = [
  { stage: '1. Service Health', question: 'Is the service active (running)?', command: 'sudo systemctl status <service>' },
  { stage: '2. Config Validation', question: 'Does the configuration parse cleanly?', command: 'test commands (-T, test config, --config.test_and_exit)' },
  { stage: '3. Port Binding', question: 'Is the expected port listening on 0.0.0.0?', command: 'sudo ss -tulpn | grep <port>' },
  { stage: '4. Network Access', question: 'Can clients establish a TCP handshake?', command: 'nc -zv <host> <port>' },
  { stage: '5. Authentication', question: 'Do credentials return HTTP 200 rather than 401?', command: 'curl -k -u <user>:<pass> <url>' },
  { stage: '6. Telemetry Ingest', question: 'Are logs being appended to disk in real-time?', command: 'sudo tail -f /var/log/<file>' },
  { stage: '7. Downstream Storage', question: 'Is data appearing in Elasticsearch and Kibana?', command: 'curl -k https://localhost:9200/_cat/indices' },
];

export const SEARCHABLE_TROUBLESHOOTING_SCENARIOS: TroubleshootingScenario[] = [
  {
    id: 'ts-1',
    title: 'Elasticsearch Won\'t Start (status=78 / Bootstrap Checks)',
    component: 'Elasticsearch',
    symptom: 'systemctl status elasticsearch shows "failed (Result: exit-code)" with code=exited, status=78.',
    rootCause: 'Binding network.host to 0.0.0.0 triggered production bootstrap checks, but vm.max_map_count is under 262144 or discovery.type is missing.',
    detectionCommand: 'sudo journalctl -u elasticsearch -n 30 --no-pager | grep -i bootstrap',
    expectedErrorSnippet: 'ERROR: [1] bootstrap checks failed. [1]: max virtual memory areas vm.max_map_count [65530] is too low',
    stepByStepResolution: [
      'Set kernel virtual memory map count: sudo sysctl -w vm.max_map_count=262144',
      'Persist across reboots: echo "vm.max_map_count=262144" | sudo tee -a /etc/sysctl.conf',
      'Verify discovery.type: single-node is present in /etc/elasticsearch/elasticsearch.yml',
      'Restart service: sudo systemctl restart elasticsearch',
    ],
    verifyFixCommand: 'sudo systemctl status elasticsearch',
    expectedFixOutput: 'Active: active (running)',
    preventionTip: 'Always configure discovery.type: single-node when setting up a single-server lab.',
  },
  {
    id: 'ts-2',
    title: 'Kibana Connection Refused (Port 5601 Unreachable)',
    component: 'Kibana',
    symptom: 'Analyst browser displays "Connection Refused" or "Site cannot be reached" on http://<SIEM_IP>:5601.',
    rootCause: 'server.host is set to "localhost" instead of "0.0.0.0", or UFW firewall is dropping incoming packets.',
    detectionCommand: 'sudo ss -tulpn | grep 5601 && sudo ufw status | grep 5601',
    expectedErrorSnippet: 'tcp LISTEN 0 511 127.0.0.1:5601 0.0.0.0:* (Notice 127.0.0.1 instead of 0.0.0.0)',
    stepByStepResolution: [
      'Open /etc/kibana/kibana.yml with sudo nano',
      'Change server.host: "localhost" to server.host: "0.0.0.0"',
      'Allow firewall port: sudo ufw allow 5601/tcp',
      'Restart Kibana: sudo systemctl restart kibana',
    ],
    verifyFixCommand: 'sudo ss -tulpn | grep 5601',
    expectedFixOutput: '0.0.0.0:5601',
    preventionTip: 'Binding to 0.0.0.0 is mandatory for all server web services accessed remotely.',
  },
  {
    id: 'ts-3',
    title: 'Filebeat Cannot Connect to Logstash (Connection Refused 5044)',
    component: 'Filebeat',
    symptom: 'Filebeat logs: "Failed to connect to backoff(async(tcp://localhost:5044)): dial tcp 127.0.0.1:5044: connect: connection refused".',
    rootCause: 'Logstash daemon is stopped, crashed, or still compiling its JRuby pipeline.',
    detectionCommand: 'nc -zv localhost 5044',
    expectedErrorSnippet: 'nc: connect to localhost port 5044 (tcp) failed: Connection refused',
    stepByStepResolution: [
      'Check if Logstash is running: sudo systemctl status logstash',
      'If stopped, start it: sudo systemctl start logstash',
      'Wait 30 seconds for JRuby to bind port 5044',
      'Inspect /var/log/logstash/logstash-plain.log to verify pipeline started',
    ],
    verifyFixCommand: 'sudo filebeat test output',
    expectedFixOutput: 'talk to host... OK',
    preventionTip: 'Logstash requires 30+ seconds to initialize before port 5044 opens.',
  },
  {
    id: 'ts-4',
    title: 'Logstash Pipeline Failed to Start (Syntax / Brace Error)',
    component: 'Logstash',
    symptom: 'Logstash starts then terminates immediately with status=1.',
    rootCause: 'Unclosed curly brace "{" or typo in /etc/logstash/conf.d/01-beats.conf.',
    detectionCommand: 'sudo /usr/share/logstash/bin/logstash -f /etc/logstash/conf.d/01-beats.conf --config.test_and_exit',
    expectedErrorSnippet: 'Expected one of #, input, filter, output at line 24, column 1',
    stepByStepResolution: [
      'Open the file: sudo nano /etc/logstash/conf.d/01-beats.conf',
      'Ensure every open brace "{" has a closing brace "}" in input, filter, and output blocks',
      'Run test command: sudo /usr/share/logstash/bin/logstash -f /etc/logstash/conf.d/01-beats.conf --config.test_and_exit',
      'Restart service: sudo systemctl restart logstash',
    ],
    verifyFixCommand: 'sudo systemctl status logstash',
    expectedFixOutput: 'Active: active (running)',
    preventionTip: 'Always run --config.test_and_exit before restarting Logstash in production.',
  },
  {
    id: 'ts-5',
    title: 'Suricata Generates No Alerts (Wrong Interface / af-packet)',
    component: 'Suricata',
    symptom: 'Attacks are launched from Kali, but eve.json remains completely empty (0 bytes).',
    rootCause: 'Suricata is listening on an inactive or non-existent interface (e.g. eth0 instead of ens33).',
    detectionCommand: 'ip -brief link && grep -A 4 "af-packet:" /etc/suricata/suricata.yaml',
    expectedErrorSnippet: 'Configured interface does not match active interface reported by ip link',
    stepByStepResolution: [
      'Run "ip -brief link" to identify your active interface name (e.g., ens33)',
      'Edit /etc/suricata/suricata.yaml and update the af-packet interface parameter',
      'Run "sudo suricata -T -c /etc/suricata/suricata.yaml" to verify',
      'Restart Suricata: sudo systemctl restart suricata',
    ],
    verifyFixCommand: 'sudo tail -n 10 /var/log/suricata/suricata.log',
    expectedFixOutput: 'af-packet: opening interface <INTERFACE>',
    preventionTip: 'Always check "ip link" in new virtual machines before configuring packet sniffers.',
  },
  {
    id: 'ts-6',
    title: 'Port Already in Use (BindException Address in use)',
    component: 'Stack Wide',
    symptom: 'A service fails to start because another process is already bound to its port.',
    rootCause: 'An orphaned or background process holds the socket handle.',
    detectionCommand: 'sudo ss -tulpn | grep <PORT>',
    expectedErrorSnippet: 'users:(("process_name",pid=XXXX,fd=YY))',
    stepByStepResolution: [
      'Run sudo ss -tulpn | grep <PORT> to identify the conflicting PID',
      'Check what process it is: ps aux | grep XXXX',
      'Terminate the conflicting process: sudo kill -9 XXXX',
      'Start your desired service: sudo systemctl start <service>',
    ],
    verifyFixCommand: 'sudo ss -tulpn | grep <PORT>',
    expectedFixOutput: 'Correct process name listening on port',
    preventionTip: 'Avoid launching standalone test commands in background jobs without killing them.',
  },
  {
    id: 'ts-7',
    title: 'Elasticsearch Authentication Failure (401 Unauthorized)',
    component: 'Elasticsearch',
    symptom: 'Kibana, Logstash, or cURL receives HTTP 401 Unauthorized.',
    rootCause: 'Incorrect password supplied for the elastic or kibana_system users.',
    detectionCommand: 'curl -k -i https://localhost:9200',
    expectedErrorSnippet: 'HTTP/1.1 401 Unauthorized\n{"error":{"root_cause":[{"type":"security_exception","reason":"missing authentication credentials"}]}}',
    stepByStepResolution: [
      'Reset the elastic superuser password to a known string: sudo /usr/share/elasticsearch/bin/elasticsearch-reset-password -u elastic -i',
      'Reset the kibana_system password: sudo /usr/share/elasticsearch/bin/elasticsearch-reset-password -u kibana_system -i',
      'Update the password in /etc/kibana/kibana.yml and /etc/logstash/conf.d/01-beats.conf',
      'Restart Kibana and Logstash',
    ],
    verifyFixCommand: 'curl -k -u elastic:YOUR_PASSWORD https://localhost:9200',
    expectedFixOutput: '"tagline" : "You Know, for Search"',
    preventionTip: 'Store lab passwords securely in your lab environment settings panel.',
  },
  {
    id: 'ts-8',
    title: 'Disk Full / Watermark Flood Stage (Read-Only Cluster Block)',
    component: 'Elasticsearch',
    symptom: 'Elasticsearch blocks writes: cluster_block_exception [TOO_MANY_REQUESTS/12/disk usage exceeded flood-stage watermark].',
    rootCause: 'Free disk space dropped below 5% (flood stage threshold).',
    detectionCommand: 'df -h /var/lib/elasticsearch',
    expectedErrorSnippet: 'Use% 96% or higher on root partition',
    stepByStepResolution: [
      'Clean temporary files: sudo apt clean && sudo rm -rf /tmp/*',
      'Delete old Elasticsearch indices: curl -k -u elastic:pass -X DELETE "https://localhost:9200/old-index-*"',
      'Unlock the read-only cluster block: curl -k -u elastic:pass -X PUT "https://localhost:9200/_all/_settings" -H "Content-Type: application/json" -d \'{"index.blocks.read_only_allow_delete": null}\'',
    ],
    verifyFixCommand: 'df -h /var/lib/elasticsearch',
    expectedFixOutput: 'Free disk space above 20%',
    preventionTip: 'Configure Index Lifecycle Management (ILM) in Kibana to delete logs older than 7 days.',
  },
  {
    id: 'ts-9',
    title: 'YAML Syntax Error (Tabs instead of Spaces)',
    component: 'All Services',
    symptom: 'ParserException: while scanning a simple key; could not find expected \':\'.',
    rootCause: 'Tab characters were used for indentation in .yml or .yaml files.',
    detectionCommand: 'grep -P "\t" /etc/path/to/file.yml',
    expectedErrorSnippet: 'Line contains tab characters',
    stepByStepResolution: [
      'Convert all tabs to two spaces: sudo sed -i \'s/\\t/  /g\' /etc/path/to/file.yml',
      'Verify indentation is consistently 2 spaces per nested level',
      'Retest with service configuration test tool',
    ],
    verifyFixCommand: 'grep -P "\t" /etc/path/to/file.yml || echo "CLEAN_NO_TABS"',
    expectedFixOutput: 'CLEAN_NO_TABS',
    preventionTip: 'In Nano, configure ~/.nanorc with "set tabstospaces" to prevent tab inserts.',
  },
  {
    id: 'ts-10',
    title: 'Wrong IP Address in Agent Shippers (Connection Timed Out)',
    component: 'Filebeat',
    symptom: 'Filebeat on edge machines fails to connect with "i/o timeout" or "connection refused".',
    rootCause: 'SIEM server IP changed due to DHCP lease renewal or wrong IP typed into filebeat.yml.',
    detectionCommand: 'hostname -I',
    expectedErrorSnippet: 'Current IP does not match the IP configured in filebeat.yml',
    stepByStepResolution: [
      'Check the current SIEM IP: hostname -I',
      'Update the IP in /etc/filebeat/filebeat.yml',
      'Test connection: sudo filebeat test output',
    ],
    verifyFixCommand: 'sudo filebeat test output',
    expectedFixOutput: 'talk to host... OK',
    preventionTip: 'Always assign a static IP address to your SIEM server.',
  },
  {
    id: 'ts-11',
    title: 'UFW Firewall Blocking Inbound Telemetry or Kibana',
    component: 'Host Security',
    symptom: 'Local curl works, but external hosts cannot reach ports 5044 or 5601.',
    rootCause: 'UFW default deny incoming policy is active without rules for ports 5044 or 5601.',
    detectionCommand: 'sudo ufw status verbose',
    expectedErrorSnippet: 'Ports 5044 or 5601 missing from ALLOW list',
    stepByStepResolution: [
      'Allow Kibana: sudo ufw allow 5601/tcp',
      'Allow Logstash Beats: sudo ufw allow 5044/tcp',
      'Reload firewall: sudo ufw reload',
    ],
    verifyFixCommand: 'sudo ufw status | grep -E "5601|5044"',
    expectedFixOutput: 'ALLOW IN for both ports',
    preventionTip: 'Check UFW status whenever a newly installed service cannot be reached remotely.',
  },
  {
    id: 'ts-12',
    title: 'Out of Memory (OOM) Killer Terminates Service',
    component: 'Host OS / Kernel',
    symptom: 'Service stops unexpectedly without writing any error to its own application logs.',
    rootCause: 'Linux kernel OOM killer terminated the JVM because system RAM was exhausted.',
    detectionCommand: 'sudo dmesg -T | grep -i oom',
    expectedErrorSnippet: 'Out of memory: Kill process (java) score ... or sacrifice child',
    stepByStepResolution: [
      'Check current memory usage: free -h',
      'Reduce Logstash heap in /etc/logstash/jvm.options to -Xms1g -Xmx1g',
      'Reduce Elasticsearch heap in /etc/elasticsearch/jvm.options.d/ to 2g',
      'Add a 4GB swap file if running on a memory-constrained VM',
    ],
    verifyFixCommand: 'free -h',
    expectedFixOutput: 'Swap available and free memory > 1GB',
    preventionTip: 'Sum of all JVM heap allocations must never exceed 50% of physical system RAM.',
  },
];
